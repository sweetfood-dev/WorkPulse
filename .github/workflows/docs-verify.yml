name: Docs Verify

on:
  pull_request:
  push:
    branches:
      - main

jobs:
  docs_verify:
    name: Docs Verify
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v5

      - name: Verify Docs
        run: make verify
